@model CatBlog.Models.Post

<h2>@Model.Title</h2>

<img src="@Model.ImageUrl"
     alt="@Model.Title"
     style="width:300px; height:200px; object-fit:cover; display:block; margin-bottom:1rem;" />

<p>@Model.Content</p>

<a href="@Url.Action("Index", "Home")" class="btn btn-secondary">Back</a>
